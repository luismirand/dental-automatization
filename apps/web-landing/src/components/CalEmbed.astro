---
import { CalReactWidget } from "./site/CalReactWidget";

interface Props {
  calLink?: string;
}

const { calLink = "luis-miranda" } = Astro.props;
---

<section id="booking" class="py-20 bg-slate-900 text-white relative overflow-hidden scroll-mt-20">
  <div id="agendar" class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    
    <!-- Section Header -->
    <div class="text-center max-w-3xl mx-auto mb-12 space-y-4">
      <span class="px-3.5 py-1 rounded-full bg-teal-500/10 text-teal-400 border border-teal-500/20 text-xs font-semibold uppercase tracking-wider">
        Reserva tu turno al instante
      </span>
      <h2 class="text-3xl sm:text-4xl font-extrabold text-white">
        Calendario de Agendamiento Directo
      </h2>
      <p class="text-slate-400 text-base">
        Selecciona la fecha y hora que mejor se adapte a tu agenda. Recibirás una confirmación inmediata por correo y recordatorio por WhatsApp.
      </p>
    </div>

    <!-- Container where Cal.com embed renders -->
    <div class="rounded-3xl bg-slate-800/80 border border-slate-700/60 p-4 sm:p-8 shadow-2xl min-h-[720px] flex items-center justify-center overflow-hidden">
      <CalReactWidget client:load />
    </div>

  </div>
</section>


