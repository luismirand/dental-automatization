# Public browser configuration. Never put secrets in PUBLIC_* variables.
# Astro's development proxy handles /api/webchat when this variable is omitted.
PUBLIC_WEBCHAT_WEBHOOK_URL=http://localhost:5678/webhook/webchat
